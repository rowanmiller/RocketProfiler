<Window x:Class="RocketProfiler.UI.Views.UploadView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RocketProfiler.UI.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="Uploading" Height="100" Width="300">
    <Grid>
        <StackPanel>
            <TextBlock HorizontalAlignment="Center" Text="{Binding Message}" FontSize="16" />
            <ProgressBar Height="30" Margin="5" Value="{Binding Progress, Mode=OneWay}" />
            <TextBlock HorizontalAlignment="Center" Text="{Binding ProgressMessage}" FontSize="12" />
        </StackPanel>
    </Grid>
</Window>
